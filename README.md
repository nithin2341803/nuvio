# NUVIO — Premium Kerala Digital World

A rebuilt, cinematic one-page NUVIO website using HTML/CSS/JS, GSAP ScrollTrigger and Lenis. Visual assets are drawn with CSS and layered shapes so the project has no dependency on uploaded image assets. Logo areas are deliberately reserved and empty.

Open `index.html` locally or deploy the folder to Vercel/static hosting.
