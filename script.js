document.addEventListener('DOMContentLoaded',()=>{const reduce=matchMedia('(prefers-reduced-motion:reduce)').matches;const root=document.documentElement,mark=document.querySelector('.openingMark'),glyph=document.querySelector('.glyphHuge'),giant=document.querySelector('.giantGlyph'),words=[...document.querySelectorAll('.industryTrack span')];let ticking=false;const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;const y=scrollY,p=Math.min(y/max,1);root.style.setProperty('--scroll',p);document.querySelector('.progress span').style.transform=`scaleY(${p})`;if(!reduce){const openEnd=innerHeight*1.15;const op=Math.min(y/openEnd,1);mark.style.transform=`translate(-50%,-50%) scale(${1+op*7}) rotate(${op*18}deg)`;mark.style.opacity=Math.max(0,1-op*.95);const t=document.querySelector('.threshold');if(t){const r=t.getBoundingClientRect();const q=Math.max(0,Math.min(1,(innerHeight-r.top)/(innerHeight+r.height*.65)));document.querySelector('.obelisk').style.transform=`translate(-50%,-50%) scale(${1+q*.7}) rotate(${q*3}deg)`}if(glyph){glyph.style.transform=`scale(${.65+Math.min(1,Math.max(0,(y-innerHeight*3.1)/(innerHeight*1.4)))*.45}) rotate(${(-12+p*16)}deg)`}if(giant){giant.style.transform=`scale(${1+p*.35}) rotate(${-p*10}deg)`}words.forEach((w,i)=>{w.style.transform=`translateX(${Math.sin(y/700+i)*18}px)`})}ticking=false};window.addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(update);ticking=true}},{passive:true});update();const form=document.getElementById('projectForm');form.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form);const msg=['Hello Nuvio, I want to start a website project.','',`Company Name: ${d.get('company')}`,`Your Name: ${d.get('name')}`,`Phone Number: ${d.get('phone')}`,`Website Plan: ${d.get('plan')}`,`Business Type: ${d.get('type')}${d.get('other')?' — '+d.get('other'):''}`].join('\n');form.hidden=true;document.getElementById('success').hidden=false;setTimeout(()=>window.open('https://wa.me/919037686996?text='+encodeURIComponent(msg),'_blank','noopener,noreferrer'),400)});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const target=document.querySelector(a.getAttribute('href'));if(target){e.preventDefault();target.scrollIntoView({behavior:reduce?'auto':'smooth'})}}));});